server {
    listen 80;
    root /usr/share/nginx/html;
    client_max_body_size 10m;
    location /api/ { proxy_pass http://api:8000; proxy_read_timeout 95s; }
    location / { try_files $uri $uri/ /index.html; }
}
