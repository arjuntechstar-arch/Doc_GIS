FROM python:3.12-slim
WORKDIR /app
COPY src/backend/requirements.txt requirements.txt
RUN pip install --no-cache-dir -r requirements.txt
COPY src/backend/ /app/
ENV PYTHONPATH=/app
EXPOSE 8000
CMD ["uvicorn", "healthcare_gis.app:app", "--host", "0.0.0.0", "--port", "8000"]
