# Existing local MongoDB defaults:
HEALTHCARE_GIS_MONGO_URI=mongodb://localhost:27017/
HEALTHCARE_GIS_MONGO_DB=dog_gis

# Only needed if you use the optional authenticated Docker Compose instance:
MONGO_ROOT_PASSWORD=
